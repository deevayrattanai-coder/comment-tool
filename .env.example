# PostgreSQL connection string
DATABASE_URL=postgres://user:password@localhost:5432/comment_creator

# A long random string used to sign auth session cookies
SESSION_SECRET=replace-with-a-long-random-string

# Port the Next.js server should listen on (defaults to 5000 if unset)
PORT=5000
